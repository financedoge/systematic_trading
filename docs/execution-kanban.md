# Execution Kanban

This is the durable build tracker for the industrial trading platform. Update it at the start and end of every implementation session.

Status values:

- `Done`: implemented, documented, and verified.
- `In Progress`: actively being worked in the current session or branch.
- `Pending`: planned but not started.
- `Blocked`: cannot proceed without an explicit external decision, credential, service, dependency, or infrastructure change.
- `Review`: implemented but needs human review, operational soak, or production evidence.

## Active session — 2026-09-26

- **Done: source publication checks (2026-09-27).** Verified the 48-file audited-research, chart-navigation and app-native strategy commit, including staged Python/JSON syntax, whitespace and Ruff. Existing full/final test receipts pass. Generated histories, broker reconciliation state and calculation outputs remain local. Remote `master` matches the starting commit; direct HTTPS timeout resolved by using the already configured Windows proxy for this Git invocation. Commit and push authorized by the user.

- **Done: app-native tracked strategy calculations and full lag-20 report (completed 2026-09-27).** Registered full executable definitions, app-owned native LEAN/Python engines, audited input/FX gates, immutable receipts, monthly signal/fill replay, daily value, held and latest indicative weights, atomic ClickHouse publication and app Refresh calculations. Deleted the Codex automation and retired its script; AGENTS/playbook require app-owned calculations. SOTA and lag-20 share full reports, matched risk parity/URTH/SOTA benchmarks, meaningful signal attribution and full zoom/pan decision diagrams. History covers 2016–2026-09-25 using causal annual trees before the deployed frozen model begins in 2023. Final three native runs passed parity; published 5,400 observations / five documents. Full suite 668 passed / 51 optional skips; final regression suite 62 passed / one optional skip; 14 final source/renderer checks; Ruff clean. Browser verified both reports, benchmark switching, held/target weights, diagrams and app refresh. Connection timeout retained the previous serving report and succeeded on app retry. All eight services healthy after a real read-only IB probe; SOTA/paper policy unchanged. Evidence: `var/research/app-tracking-acceptance.json`, `app-tracking-*-tests.xml`; protocol: `docs/etf-activity-lag20-tracking.md`. Track only; no promotion.

- **Superseded: initial ETF lag-20 research card and agent reviews.** The first frozen 935-point pair remains immutable historical evidence in ClickHouse. Its Codex automation has been deleted and review script retired in favor of the app-native executable strategy above. Do not restart the agent workflow. Original verification: `var/research/activity-tracking-*`.
- **Done: audited-input policy and consistent chart navigation.** AGENTS.md and the research playbook require published audited continuous inputs, pinned hashes and explicit bases. Market History combines Audited Series and Recorded Bars, with Raw Data below; audited history and trading performance default to full history. Shared drag zoom, Pan/Shift-drag, bounded ranges, cancellation and reset cover market/source charts, performance, P&L/slippage, archived strategies and exported reports. Browser verified real market/source/performance/report zoom and pan; normalization and transformed-pointer tests pass. Full suite: 652 passed / 51 optional integrations skipped; Ruff clean. Normal guarded deployment recovered after transient NAS reconnect/backup lock; all eight services healthy after a real IB probe, prior paper policy identical. Guide: `docs/market-history-and-chart-navigation.md`; receipts: `var/research/audited-ui-*`.
- **Done: audited-input strategy and HHI reruns; Review: future coverage and ETF lag-20 observation.** All 24 frozen native LEAN trials passed parity, including 10.7-year annual-causal comparisons, cost/delay stresses and actual-model 2023 companions. Baseline 9.84% CAGR / 0.956 Sharpe; early signed 9.82% / 0.955 / IR -0.172; concentration IR -0.289. ETF lag-20 9.99% / 0.966 / IR +0.285 remains uncertain (family-adjusted p=0.574). Only 32/129 constituent decisions qualify under unchanged audited raw/identity coverage; first August 2023, so earlier neutral periods are not a decade of constituent validation. Regenerated 19 scatter grids on 551 common dates. Exact-readback ClickHouse publication: 11,862 observations / 22 documents; 24 PostgreSQL run registrations. Frozen outputs and findings: `docs/audited-research-rerun-2026-09-26.md`, `D:/systematic_trading_data/lean/research/audited-rerun-20260926-v1/`. Retain SOTA; resolve historical issuer/raw-price gaps and test the fixed ETF candidate prospectively before promotion.

- **Done: source publication verification.** Completed analytical/research/governance/chart changes passed 645 tests with 51 optional integration skips, Ruff, staged Python syntax/import closure and whitespace checks. Source, tests, configurations and documentation are staged together; generated data and runtime state remain local. Test receipt: `var/research/pre-push-20260926-tests.xml`.

- **Done: interactive governed time-series charts.** Each underlying opens at the full saved range. Drag either chart to zoom; Pan mode or Shift+drag moves the window, with synchronized comparison dates and visible-value price scaling. Full history/Home resets; arrow keys pan and Esc cancels. Date fields, displayed rows and JSON exports follow the viewport; basis/source changes preserve it. Nulls and audit gaps stay missing. Twenty-five focused tests and Ruff passed; real-browser drag, reverse selection, pan, reset, date entry and AAPL/HYG/GE/ASRT checks passed without console errors. Standard guarded restart deployed the view; paper policy byte-identical. Evidence: `var/research/governed-chart-tests.xml`, `governed-chart-ui-verification.json`.

- **Done: governed research price histories and Market Data comparison view; Review: unresolved identities, missing histories and governed-input backtest reruns.** Published policy-v2 batch `69755461f6f8088675229e9aff0f7ee8cbc2cf5a8083e126e58252e7f22dd5e3`: 5,340 symbols, 4,537 available histories, 23,295,861 daily rows, 55,169,737 original comparison rows, 228,262 source-vintage action records and 20,310 per-symbol evidence documents. Every row/document hash and whole-batch count passed ClickHouse readback; independent API acceptance and browser checks passed. Raw-price reconstruction supports 17,825,179 rows; 803 symbols remain unavailable, 1,311 histories have stale/delisted-unverified endpoints, and 772,141 original observations across 686 symbols are quarantined before identity/start boundaries. All 12 strategy ETFs have zero internal gaps and both price bases through September 25. V1 was rejected before catalog publication after reused-ticker contamination was found; originals retained and old-policy publication refused. Prior ETF inputs contain material source/adjustment seams, so previous strategy metrics require rerunning before promotion. Full suite 643 passed/51 optional skips; nine final API tests and Ruff passed; paper policy byte-identical. Guide: `docs/price-governance-2026-09-26.md`; frozen data/review: `D:/systematic_trading_data/research/governed-prices-20260926-v2/`, `governed-prices-audit-20260926-v2/`. No strategy or trading-reader switch.

- **Done: ten-year constituent evidence and Market Data visibility; Review: prospective evidence and certified histories.** Added 170 dated IVV snapshots, 2,298,993 current-provider stock observations and 7,595,897 archived public observations with exact ClickHouse readback. LEAN now tests 2016-01-04–2026-09-24 with 122/129 qualified monthly decisions; 2020/2022 are covered. Annual expanding base trees avoid replaying the pre-2023 fitted tree into its training past. Twenty-two long runs plus four actual frozen-model companion runs passed native parity. Long baseline CAGR/Sharpe 9.64%/0.932; early signed 9.68%/0.936/IR +0.158 trails price control 9.69%/0.937/+0.190. Joint tree adds only 0.002pp CAGR/direct IR +0.039 over price-only; concentration IR -0.232. No promotion. Market Data now has a verified Research Data tab with filters, source details, charts and page exports; approval policy unchanged. Full suite 616 passed/51 optional skips, Ruff clean, browser and plots inspected. Source/model/result evidence in ClickHouse and 26 successful native receipts in PostgreSQL. One stalled native attempt retained and identical-input retry passed. Guide: `docs/constituent-tenyear-research-2026-09-26.md`; root: `D:/systematic_trading_data/lean/research/constituent-tenyear-20260926-v2/`.
- **Done: earlier integration of positive-sample-IR constituent signals; Review: ETF-specific panel and prospective evidence.** Nineteen native LEAN runs passed, including exact prior SOTA/late-reference hashes. Post-2023 signed early/late both return 14.77% / Sharpe 1.299 / IR +0.262, behind earlier price-only 14.79% / 1.301 / +0.301. Signed selection and half-strength change zero allocations; breadth selection changes one and loses return. Joint tree uses actual stock splits but 2025-onward CAGR 18.84% / Sharpe 1.467 / IR -0.876 trails SOTA 19.03% / 1.474 and price-only tree 18.89% / 1.471. Small annual fits (23/35 months), reused sample and public membership/vintage limitations preclude promotion; retain SOTA. ClickHouse retains 3,592 features, four models, 43 separate training labels, 252 result/contrast records and documents; all 19 receipts registered. Full suite 599 passed/51 optional skips; focused 55 passed/1 skip; Ruff clean and charts inspected. Guide: `docs/constituent-integration-research-2026-09-26.md`; immutable inputs/native runs: `D:/systematic_trading_data/lean/research/constituent-integration-20260926-v1/`.
- **Done: constituent signals as SOTA ETF overlays in LEAN; Review: better membership/history and prospective evidence.** Fifteen predeclared native runs passed unchanged parity. Full-period primary composite CAGR 8.49% / Sharpe 0.782 / IR -0.394 versus SOTA 8.52% / 0.784; concentration IR -0.507. Usable stock coverage begins January 2023: primary post-2023 IR -0.540, signed activity +0.262 but weaker than price-only +0.299 and family-adjusted p=0.831. Only 44/84 monthly decisions qualify; primary changes 33. Half-size, holdings-delay, lower-coverage, high-cost and execution-delay checks do not rescue the primary. Retain SOTA; no promotion or trading-policy change. Existing 5,923,052 stock bars remain in ClickHouse, plus 3,592 new feature records and 180 result metrics with exact hash readback; all 15 runs registered. Full suite 589 passed/51 optional skips; focused 45 passed/1 skip; Ruff clean and charts inspected. Frozen artifacts: `D:/systematic_trading_data/lean/research/constituent-signals-20260926-v1/`; results and staged signal/data plan: `docs/constituent-signals-research-2026-09-26.md`.
- **Done: underlying-stock sector concentration and ClickHouse archive; Review: historical coverage limits.** Public pilot froze 95 monthly ITOT snapshots, 3,267 populated stock histories, 32 empty responses and all 1,858 unavailable/rejected requests; issuer-verified aliases preserve original identities. Produced 43 charts of stock-aggregated sector turnover/returns and coverage. Only 139 common dates pass 95%-value/70%-name rules and complete all horizons; raw derivative correlations are near zero, insufficient for market-wide/strategy conclusions. ClickHouse stores 5,923,052 stock bars, 299,575 holding rows, 5,157 endpoint statuses, 85,492 aggregate/label records, 8,696 source documents and prior 24,936 ETF bars. Exact payload hashes verified before publication; counts and SQL retrieval checked; ETF/analysis repeats write nothing. Eighteen focused tests and Ruff passed; main plots visually inspected. Guide: `docs/underlying-sector-hhi-2026-09-26.md`; artifacts: `D:/systematic_trading_data/research/underlying-sector-hhi-20260926-v1/`; receipts: `var/research/*clickhouse*.json`. Retain SOTA; missing delistings, permanent IDs and historical publication vintages remain unresolved.
- **Done: sector-volume HHI correlation plots.** Froze Yahoo daily history for all 11 sector SPDRs and SPY (2,078 complete sessions); produced 19 scatter grids, timeline, sector heatmap, selectable gallery and replayable observations. Common signal dates 2019-01-02–2026-04-02 (1,823 dots) have observed 120-session outcomes. SPY raw HHI correlations at 20/60/120 sessions are +0.075/+0.115/+0.093; daily derivatives are near zero. Smoothed acceleration remains weak; dollar-volume and period checks weaken the level result. Overlapping labels, ETF units/splits and lack of actual net flows remain explicit. Eight focused tests and Ruff passed; charts visually inspected. Evidence: `D:/systematic_trading_data/research/sector-hhi-20260926-v1/`; guide: `docs/sector-hhi-correlations-2026-09-26.md`. No strategy or trading-policy change.
- **Done: flow/concentration acceleration research with LEAN; Review: fixed lag-20 observation candidate and missing net-flow data.** Completed 17 predeclared native runs plus two explicitly post-hoc cost/delay checks; all passed unchanged parity. Primary CAGR 9.06% / Sharpe 0.919 versus SOTA 9.11% / 0.924 (2013-04-01–2026-09-24). Best lag-20 neighbor 9.28% / 0.935 survives stresses but family-adjusted p=0.354 and weak neighborhood stability do not justify promotion; literal gate returns 2.90% with 64.8% cash. Full pytest passed; final focused checks 38 passed/1 optional integration skipped; Ruff clean. Data audit, literature, formulas, all trials, bootstrap and inspected charts: `docs/flow-concentration-research-2026-09-26.md`, `D:/systematic_trading_data/lean/research/flow-concentration-20260926-v3/`. Missing historical shares/NAV/net issuance/holdings/sector data documented. Keep SOTA and broker controls unchanged; next evidence is timestamped true-flow data and an untouched prospective comparison.
- **Done: analytical time-series migration and Strategy performance.** Broad analytical scope published to ClickHouse with verified row hashes, versioned commits, incremental account/raw imports, preserved source evidence and transactional PostgreSQL authority. Strategy/account performance reads saved documents; background refresh detects source/backfill changes and protects reset boundaries. Catalog 11.63s → 0.01–0.09s; full report 36.29s → 0.11–0.14s; 3,704 chart points match exactly. Full suite 548 passed/51 optional integrations skipped; final affected suite 65 passed including real ClickHouse verification. Worker first refresh succeeded without errors; policy revision 1 unchanged and reconciliation matched. Guide: `docs/analytics-migration.md`; evidence: `var/migration/analytics-*`, `analytical-*`, `strategy-report-parity.json`, `pnl-projection-parity.json`.
- **Done: URTH historical benchmark gap repair.** Restored 92 observed sessions (April 30–September 10) from Yahoo adjusted bars. Recurring backfill includes benchmarks absent from storage and expands to historical holes; incomplete benchmark coverage produces a nonzero child status. 38 regression tests and Ruff passed; repeat repair through September 24 inserted zero rows. Evidence: `var/urth-benchmark-repair.json`, `var/urth-benchmark-repeat.json`, `var/urth-report-verification.json`. September 25 remains provider-blocked (Yahoo absent, IB history timeout); recurring cycles retry. Fresh backfill subprocesses pick up the fix without a service restart.
- **Done: PnL N/A follow-up.** Retain last received broker values with explicit stale timestamps, render partial account/position fields independently and survive failed browser refreshes. Independent, bounded dashboard panel loading prevents slow performance/reconciliation requests hiding attribution; request-scoped ledger/FX/price read reuse reduced measured attribution latency from 87.14s to 3.23s with identical totals. 75 regression tests passed; full Ruff clean. Deployed dashboard/dispatcher 33380/32148; real browser shows all six position daily PnLs and account HKD 1,208.38 (stale), accounting CNH 977.08 and reference CNH 1,960.12. Reconciliation matched; existing paper policy unchanged. Evidence: `var/pnl-na-before.json`, `var/pnl-na-after.json`, `var/pnl-na-regression.xml`.
- **Done: live broker P&L.** Dedicated read-only IB stream, verified account/currencies, freshness/unavailable states and reconnect tests. Deployed; real browser/API showed changing PnL and six positions. Stored daily accounting remains separately labelled.
- **Done: platform audit and corrections.** Point-in-time signals/filings/labels, prior-session execution FX, missing-price/calendar/valuation failures, completed daily data and lint fixes. Final full suite: 579 passed, one optional SMB test skipped; Ruff E9/F clean. Current delayed recorder and recent IB FX history verified working. See `docs/platform-audit-2026-09-26.md` for remaining data/entitlement/research limitations.
- **Done: LEAN v1 research worker.** Frozen manifests/source, official pinned offline engine, replay/shared signals, native fills and Python parity, runtime/robustness evidence and append-only research registry. Twelve native runs passed, including both 3,330-session histories and identical replay. `docs/lean-validation-2026-09-26.md` and `D:/systematic_trading_data/lean/validation/20260926-v1/` retain evidence. NAS registry backup completed. Historical vintage/FX/universe certification, raw corporate actions, settlement and TWAP remain explicit later promotion gates; no LEAN routing or promotion enabled.

## Operating Rule

Before starting work:

1. Read `docs/industrial-platform-plan.md`, `docs/execution-kanban.md`, and `log.md`.
2. Pick the highest-priority `Pending` or `Review` item that is not blocked.
3. Move it to `In Progress`.
4. Record assumptions or blockers in the item notes.

After finishing work:

1. Move completed items to `Done` only after tests or equivalent verification pass.
2. Add evidence paths, commands, or reports.
3. Update dependent items if their status changed.
4. Add a concise entry to `log.md`.

## Current Snapshot

### 2026-09-26 PostgreSQL relocation command repair

Status: **Done (repair, tests and service recovery); Review (elevated relocation retry)**. Replaced sc.exe serialization with Win32_Service.Change via structured CIM arguments, return-code validation and exact command readback; rollback uses the same helper and is armed before the change attempt. Tests execute helper with mocked service calls under Windows PowerShell 5.1 and PowerShell 7, covering quoted forward/rollback paths, rejection and readback mismatch; combined operator tests: 3 passed. PostgreSQL confirmed on C:, original D: copy confirmed shut down/same cluster and preserved as data.failed-sc-20260926 after checked-path rename. Standard startup restored required services and matched paper reconciliation; recorder separately reports IB 10197 competing session. NAS preflight passes. Retry same script in Administrator PowerShell; no move completion claimed.


### 2026-09-25 elevated NAS relocation preflight

Status: **Done (preflight repair and service recovery); Blocked (elevated NAS access / physical PostgreSQL move)**. Confirmed no PG stop/copy/retarget; normal session sees matching NAS ownership/history. Suspected elevated SMB-session visibility, with transient outage also possible. Added same-process preflight before downtime; checks access, exclusive lock and active/clean handoff identity without taking ownership or modifying state. Standard startup restored all eight services healthy and matched paper reconciliation. NAS/operator tests: 20 passed, 1 optional SMB test skipped; real normal-session preflight and PowerShell syntax checks passed. Retry from Administrator PowerShell only after NAS connection/preflight succeeds. No guard bypass, credential changes or broker commands.


### 2026-09-25 database consolidation and LEAN plan

Status: **Done (SQLite retirement, Docker D: relocation and LEAN plan); Blocked (PostgreSQL physical move: Windows administrator access)**. Default application/research/worker/reporting paths use PostgreSQL/ClickHouse; explicit SQLite remains for tests and retained recovery/NAS-layout compatibility. Archived all 145,836 SQLite rows into PostgreSQL with exact readback; verified operational identity/approval coverage and all 140,686 bar / 3,790 FX keys in ClickHouse, preserving newer state. Moved Docker WSL disks and both database/event volumes to `D:/systematic_trading_data/docker/DockerDesktopWSL` after stopped-volume tar backups and clean NAS handoff. Restarted platform; all eight services healthy and IB paper reconciliation matched. Read-only server-store backtest: 250 NAV points, eight proposals. Full suite: 525 passed, 1 skipped; final cutover-focused checks: 21 passed. PostgreSQL service access preflight returned Windows error 5; its C: data directory remains unchanged. Prepared/parsing-checked `scripts/move_postgres_data_to_d.ps1` for elevated execution; no admin move claimed. See `docs/database-consolidation.md` and `docs/lean-backtest-integration-plan.md`. LEAN implementation L1/L2 remains Pending; no engine installed, promotion or broker order command issued.


### 2026-09-25 fill prices, TWAP benchmarks and optional paper auto-approval

Status: **Done (implementation, deployment and P&L reset); Review (IB minute-history availability and supervised automatic paper cycle)**. Added average fill, observed minute-bar TWAP estimate, signed bps/price cost and coverage; audited machine/profile/strategy-bound paper approval/submission policy remains off. New-only eligibility, cap, exact sizing/data freshness, reconciliation, regular session, pending/uncertain order and duplicate gates remain. Atomic expected-status approvals cover SQLite/PostgreSQL. Full suite: 522 passed, 1 skipped; final focused checks: 38 passed. Real browser verified six average fills, manual mode/review dialog and reset performance start. Operator/dispatcher restarted to 37548/35860; Gateway/recorder/NAS preserved. No broker orders placed/amended/cancelled and auto mode was never enabled.

Additional authorized scope: reset active P&L before September 25 using the verified flat pre-trade cash snapshot, preserving today's executions/holdings and all audit history. Baseline `d229466e93af` applied at cutoff `2026-09-25T03:59:59.999999Z`; retained 48 executions and six holdings. IB US HMDS is currently disconnected, so benchmark values require restored complete minute history; no synthetic substitute.

### 2026-09-25 order blotter dates and completed-order visibility

Status: **Done**. All six initial TWAP orders fully filled (48 executions; six positions; reconciliation matched); they were hidden by the former Working default. Blotter now defaults to Today plus All statuses, with Last 7 days / Date range / All dates and separate Filled / Working / Needs attention / Completed / Missed filters. Added submission/update timestamps, local timezone labels, New York trading-date semantics, newest-first ordering, filtered counts/audit details, and visibility for unknown-date external working orders. Old missed records retain their intended dates and remain available in history. Completed snapshot quantity zero falls back to the durable order quantity; local full fills override stale working observations. UI tests: **9 passed**, including Node date/DST/midnight, status, retention, sorting and quantity/action checks. Browser confirmed Today shows six fully filled orders with correct totals; All dates + Missed shows 220 historical records. Deployed dashboard/dispatcher 39140/32936; no broker orders or ledger history changed.

### 2026-09-25 active TWAP reconciliation lag

Status: **Done**. Operator retry successfully submitted all six TWAP orders. Reconciliation now atomically persists its exact fresh execution batch before comparing the subsequent portfolio snapshot; the API and management loop use this mode. Diagnostic synchronization lag has a separate `execution_sync_pending` state, still blocks new routing/reset, and no longer asks for audited history recovery. Genuine identity/correction/gap conflicts remain durable blocks. UI polls the latest reconciliation every 15 seconds and rejects older responses. Verification: existing affected suites **95 passed**; new SQLite/PostgreSQL sync/conflict/reset tests **10 passed**; UI suite including Node behavior test **7 passed**. Dashboard/dispatcher restarted (6512/31132); Gateway and recorder retained. At 13:57 UTC reconciliation matched 30 executions and six positions with no differences or execution issues; all six TWAP orders partially filled. No agent order submission, amendment, cancellation or baseline reset.

### 2026-09-25 proposal readiness FX repair

Status: **Done (implementation, deployment and proposal generation); Review (operator paper submission)**. Upgraded to the pinned official IB SDK 10.45.1 with callback/cancellation compatibility, observed USD/CNH and matched-date HKD/CNH cross rates, source evidence and closed-session enforcement. Required FX refresh now precedes allocation readiness; routine EOD avoids repeatedly scanning legacy zero-volume history. Fully attempted legacy approvals covered by the reconciled baseline no longer block initial allocation. Proposal/readiness polling refreshes every 15 seconds. Generated `initial-9f6f68b03399-20260925` for six TWAP buys; September 24 EOD completed.

The operator approved the proposal during verification. Submission failed before the first broker send because Gateway returned ID 1, colliding with restored order history. Routing now allocates above both Gateway's next ID and retained local IDs, keeps the unique constraint, and returns a structured stop/review message on failure. Approval/history remain intact. Full suite before the ID repair: **465 passed, 1 skipped**; final focused FX/UI/API/routing/management suite including SQLite/PostgreSQL handoff regressions: **128 passed**. Dashboard/dispatcher and SDK-dependent recorder restarted; final dashboard PID 6216. Fresh Gateway check at 13:42 UTC: zero orders and zero submission records for the approved proposal; expiry 14:05 UTC. No agent broker submission, amendment or cancellation. Operator can review and retry missing orders within the window.

### 2026-09-25 empty-portfolio initial allocation

Status: **Done** (implementation/deployment; runtime allocation blocked by FX readiness). Empty IB portfolios stage initial TWAP proposals; invested portfolios compare actual weights with active targets using the user-selected 2 percentage point per-holding threshold. Approved/current-month targets or point-in-time scheduled month-end targets preserve monthly signal updates. Current-session TWAP when a full window fits, otherwise next session; explicit approval, same-date prices/FX, reconciliation, order uncertainty and duplicate guards remain. Added atomic insert-once proposals, target lineage, visible drift/readiness status and monthly-versus-alignment conflict prevention. Full suite: 451 passed, 1 skipped; final focused service/allocation checks: 54 passed; new monthly conflict/UI checks: 2 passed. Dashboard/dispatcher restarted; recorder/NAS ownership preserved. Runtime threshold 0.02, matched reconciliation, zero holdings; no broker submission. USD/CNH remains Sep 23 and HKD/CNH Apr 29, so observed FX refresh is the next operational blocker. Details: `docs/trading-operations.md`.

### 2026-09-25 Gateway trading operations workspace

Status: **Done (implementation and deployment); Review (supervised paper lifecycle)**. Redesigned Trading operations with a 15-second Gateway order blotter, broker portfolio/cash, filters, freshness labels and review dialogs. Added same-client paper-only cancel, plain-limit amendments within approved quantity/price, and confirmed-zero-fill cancelled-order resubmission. Atomic lifecycle claims/audits preserve concurrent execution evidence across SQLite/Postgres; pending/timeouts and broker fill evidence block retries. Completed callbacks match the previously observed permanent ID. Existing approvals, reconciliation, expiry and live-disabled gates remain. Full suite: **406 passed, 1 skipped in 170.91s**; browser desktop/mobile, synthetic amendment and real Gateway snapshot/portfolio checks passed. Reconciliation matched, zero current broker orders/positions, 238 local records retained; ten historical records need terminal-status review. Operator/dispatcher restarted; recorder stayed running. No broker orders submitted/amended/cancelled. See `docs/trading-operations.md` for limitations and the supervised test sequence.

### 2026-09-25 IB Gateway cutover and engine assessment

Status: **Done (configuration, recorder fixes and assessment); Review (operational soak)**. Shared paper profile now uses authenticated IB Gateway on 127.0.0.1:4002 for orders, executions, account snapshots, reconciliation, health and recording. Local D: storage policy is honored by recorder/catalog/replay CLIs. Historical canary captured 60 bars across five symbols; the corrected 45-second delayed stream captured 16 new bars with advancing timestamps. Warning 2176 is retained and fractional-volume rounding is flagged without prematurely stopping capture. Reconciliation matched and Gateway health passed; no order was submitted. Full suite: 365 passed, 1 skipped. See `docs/ib-gateway-operation.md` and `docs/trading-engine-assessment.md`; recommend a bounded LEAN backtest pilot, not an engine replacement. Follow-ups: approved paper order/Read-Only API check, restart/session soak, API version upgrade, raw NAS backup, and EOD FX freshness. EOD retries can delay the serial management loop heartbeat; it recovered and reconciled again, but USD/CNH through September 23 leaves September 24 PnL incomplete. Preserve freshness checks.

### 2026-09-25 Docker socket recovery

Status: **Done (Docker recovery and platform startup)**. Preserved inaccessible runtime sockets by renaming the stopped Docker runtime directory and socket-only secrets-engine directory. Docker engine 29.5.3 responds; NATS and ClickHouse containers are healthy. Standard startup completed; operator/platform pages return HTTP 200. Imported 140,686 SQLite daily bars and 3,790 FX rates into ClickHouse (daily-bar batch 1,000 avoids the partition-per-insert limit); final daily-bar count is 140,816 across 39 symbols, latest 2026-09-24. Paper TWS 7497 is unavailable, so trading management is degraded. Recorder is healthy while idle; its old E:/Z: storage policy still needs local path configuration before intraday capture. NAS backup worker is running. Explicit post-start backup remains pending on NAS I/O despite reachable SMB; preserve active ownership and its operation lock until it finishes. No factory reset or container-volume deletion.

### 2026-09-25 receiving-PC database handoff

Status: **Done (database setup/restore; Docker blocker subsequently resolved above)**. NAS access restored. Verified both source snapshot SHA-256 checksums. Provisioned PostgreSQL 18 database `systematic_trading`, non-login owner and separate app/migrator/readonly roles with generated local credentials; verified migrator owner-role access and app login. Standard NAS prepare restored PostgreSQL/SQLite: all 14 PostgreSQL table counts match the source verification report and SQLite integrity is `ok`. Paper mode remains selected. Docker fails initializing its `dockerInference` socket; automatic approval review blocked removing the stale socket. Dashboard startup with SQLite market data was rejected by the existing PostgreSQL/ClickHouse contract, so no fallback was activated. Clean platform stop completed; final NAS backup published and ownership released. Next: recover Docker, use standard platform startup, seed ClickHouse from restored SQLite, verify service health, and configure paper TWS (7497 currently unavailable).

### 2026-09-25 publish NAS handoff and refresh backup

Status: **Done (published and backup verified)**. Published migration code as `48e49d0` on `origin/master`. Refreshed NAS snapshot `218c1db999494848abcfaf47944e5a35`; restored it into disposable PostgreSQL and SQLite and verified all table contents. Source state is clean, services stopped, NAS ownership released. Verification report is on the NAS under `verification/218c1db999494848abcfaf47944e5a35.json`. Exact staged-code regression: **356 passed, 1 skipped in 253.22s**. PowerShell parsing and staged whitespace checks passed. New-PC setup is documented in `docs/database-sync.md`; local infrastructure/credentials and ClickHouse data still need provisioning. Unrelated edits remain local.

### 2026-09-21 NAS database handoff

Status: **Done (implementation and isolated restore validation)**. Added single-active-workspace PostgreSQL/SQLite snapshots at `\\192.168.1.32\Public\systematic-trading`, startup restore/conflict detection, a five-minute backup worker and final stopped-service handoff. No automatic takeover or divergent-history overwrite. See `docs/database-sync.md` for provisioning, recovery and ClickHouse exclusions. Full regression run: **350 passed, 1 skipped in 251.98s**; final focused sync/storage/launcher run after additional safeguards: **25 passed, 1 skipped**. Actual NAS smoke: **12 passed**, including two-workspace SMB handoff and disposable PostgreSQL restore; evidence retained in `\\192.168.1.32\Public\systematic-trading-sync-smoke-4120c99563bb47b9b88412265decb7e0`. PowerShell parsing, Python compilation and diff checks passed. Activation occurs at the next clean platform restart; no production restore, service restart or broker action was performed. ClickHouse/raw data and local credentials still require separate provisioning/migration.

### 2026-09-20 dashboard repair completion

Status: **Done (implementation, browser and exact staged-snapshot verification)**. Completed P6.14: reset provenance survives PnL collapse, non-SOTA holdings remain in account NAV, chart alignment stays fixed across selection, and empty/sparse data plus month-end ranges behave correctly. Focused tests: **15 passed**. Full isolated staged suite: **339 passed in 220.72s**, including disposable Postgres and Node.js behavior checks. Synthetic browser checks passed desktop and 390px layouts, keyboard inspection, drag selection, Escape-to-All and empty/single-series views. Syntax and staged diff checks passed. No operational service restart, broker access or audit rewrite occurred. Dashboard changes are ready for the authorized commit/push; unrelated backfill, research and runtime artifacts remain outside the commit.

Publication check: **Done (exact staged-snapshot verification)**. The completed execution-history, reviewed-recovery, monthly-scheduling and recorder/local-service repairs passed all **331 tests in 224.17s**, including real disposable Postgres, from an isolated export of the staged tree. Syntax and staged diff checks passed. Commit/push to `origin/master` is authorized; unrelated dashboard/backfill work and runtime artifacts remain outside this commit.

### 2026-09-20 recorder calendar and local service bindings

Status: **Done (implementation and isolated verification)**. Recorder capture shares the US exchange holiday calendar, observes scheduled early closes, and refreshes session state after blocking backfills. NATS/ClickHouse published ports bind to loopback in Compose. Focused recorder/calendar/operator checks: **42 passed**. Full suite with disposable Postgres: **331 passed in 235.31s**. Resolved Compose configuration verified all four loopback bindings; Python syntax and `git diff --check` passed. Existing working-copy changes were preserved. No services were restarted or containers recreated, so existing runtime bindings await a maintenance restart/recreation. Exceptional calendar closures and a hard child-process close deadline remain outside this batch.

### 2026-09-20 audited execution recovery

Status: **Done (implementation and isolated verification)**. Added `scripts/recover_ib_paper_executions.py`: read-only preview, reviewed-state token, atomic paper-only apply, durable before/after evidence and fresh reconciliation after recovery. PnL baseline commits reject stale execution or parent-baseline state. Existing baselines and live orders cannot be rewritten through recovery. **302 full-suite tests passed in 228.85s**, with real disposable Postgres enabled via `ST_TEST_POSTGRES_BIN`; **2 final CLI checks passed** after adding the database-selection guard. The focused SQLite/Postgres recovery suite passed 31 cases including concurrent reservations, fill accumulation, stale reviews and outbox rollback. Syntax and `git diff --check` passed. Temporary test databases were stopped; the trading database and existing services were untouched. Remaining: controlled IB paper validation, zero-fill bust recovery, historical baseline rebuilds and definitive handling of unfilled uncertain submissions.

### 2026-09-20 execution history and scheduling batch

Status: **Done (implementation and local verification)**. Broker execution identities and cumulative quantities persist atomically with order/outbox updates. Duplicate, overlapping and short history responses retain prior fills; conflicts create durable routing blocks. PnL and reconciliation use execution timestamps. Automatic staging follows the registered monthly cadence and the shared US session calendar, while daily EOD reporting retains data/reconciliation gates. Full suite: **271 passed in 197.10s**; final targeted API/operator/history/scheduler checks: **64 passed**; three operator reconciliation states passed a JavaScript rendering check. Syntax and `git diff --check` passed. No historical rewrite, service restart, broker submission or migration occurred. Postgres/IB paper integration and audited correction/recovery tooling remain Review/Pending.

### 2026-09-19 review repair batch

Status: **Done (implementation and isolated verification)**. Fixed CLI storage routing and invalid backend combinations; shared reconciliation enforcement; atomic order reservations and uncertain-retry blocking; decision-date backtest sizing and native-cash affordability; broker-reset PnL baseline preservation; and prevention of synthetic carry-forward writes. Added isolated test defaults and 23 regression cases in `tests/test_review_regressions.py`. Final full suite: **240 passed in 189.33s**, using `.venv/Scripts/python.exe -B -m pytest -o addopts='' -q -p no:cacheprovider --durations=5`. Python syntax checks and repository-configured `git diff --check` passed. Existing working-copy changes were preserved. No broker submission, service restart, migration, or research promotion occurred. External integration evidence remains a separate Review item below.

Review follow-ups (not closed by this batch):

- **Implemented in the 2026-09-20 batch:** persist and reconcile execution IDs so partial broker-history responses cannot reduce cumulative fills. **Active paper recovery implemented above. Pending / P1:** historical baseline rebuilds, zero-fill bust recovery and definitive broker order outcomes to resolve unfilled uncertain submissions safely.
- **Implemented in the 2026-09-20 batch:** align automatic paper proposal staging with the registered monthly strategy scheduler.
- **Implemented in the recorder/calendar batch:** shared session calendar and local Docker loopback configuration (runtime application pending container recreation). **Pending / P2:** enforce golden-source precedence and point-in-time availability.
- **Isolated Postgres verification done:** reservations, concurrent fills, recovery, stale-state refusal and outbox rollback. **Review:** controlled IB paper recovery tests before deployment.
- **Pending:** provider-backed repair of legacy synthetic data and regeneration/re-audit of research artifacts affected by backtest sizing.

| Area | Done | In Progress | Pending | Blocked |
| --- | ---: | ---: | ---: | ---: |
| Project memory and governance | 6 | 0 | 1 | 0 |
| Event spine and local operations | 8 | 0 | 0 | 0 |
| Service foundation | 9 | 0 | 0 | 0 |
| Market data recording | 4 | 3 | 2 | 1 |
| Research/backtest promotion | 3 | 0 | 6 | 1 |
| Portfolio and execution controls | 1 | 2 | 6 | 1 |
| Observability and alerts | 11 | 0 | 6 | 1 |
| Daily operating loops | 0 | 0 | 6 | 0 |

## P0: Project Memory And Governance

| ID | Status | Work Item | Acceptance Criteria | Evidence / Notes |
| --- | --- | --- | --- | --- |
| P0.1 | Done | Industrial target-state plan | System chart, service boundaries, daily loop, and execution schedule are documented. | `docs/industrial-platform-plan.md` |
| P0.2 | Done | README project goal memory | README states industrial platform goal and links target-state plan. | `README.md` |
| P0.3 | Done | Live-disabled-by-default policy | Live rollout doc preserves paper-first gates and minimum live checklist. | `docs/live-rollout.md` |
| P0.4 | Done | Agent operating rules | Agent rules define required reading, promotion discipline, and log updates. | `AGENTS.md` |
| P0.5 | Done | Recurring operating playbooks | Daily post-trade, research, robustness, and promotion playbooks exist. | `.agents/skills/` |
| P0.6 | Done | Kanban execution tracker | This file exists, is linked from project memory, and becomes mandatory for future sessions. | `docs/execution-kanban.md`; linked from `README.md`, `AGENTS.md`, and `docs/industrial-platform-plan.md`. |
| P0.7 | Pending | Decision log discipline | Every architecture or production-control decision gets a dated `log.md` entry. | Ongoing habit, not a one-time task. |

## P1: Event Spine And Local Operations

| ID | Status | Work Item | Acceptance Criteria | Evidence / Notes |
| --- | --- | --- | --- | --- |
| P1.1 | Done | Versioned platform event contracts | Typed contracts exist for market data, features, proposals, orders, fills, reconciliation, alerts, and incidents. | `src/systematic_trading/domain/events.py`; `tests/test_events.py` |
| P1.2 | Done | Durable local event outbox | SQLite stores unpublished events, attempts, errors, and published state. | `platform_event_outbox`; `tests/test_event_outbox.py` |
| P1.3 | Done | Transactional event producers | Proposal creation, approval decisions, broker order status changes, and aggregate fills emit outbox events. | `src/systematic_trading/storage/sqlite.py` |
| P1.4 | Done | Local outbox dispatcher | JSONL publisher and dispatch script can drain pending events once or continuously. | `scripts/dispatch_event_outbox.py`; `var/events/platform_events.jsonl` |
| P1.5 | Done | Run dispatcher with operator stack | Dashboard startup process can run or supervise the outbox dispatcher. | `scripts/start_operator_dashboard.ps1` starts dispatcher unless disabled; `scripts/stop_operator_dashboard.ps1` stops it. Smoke tested on port 8765. |
| P1.6 | Done | Event replay reader | JSONL or outbox events can be replayed into typed event objects for audit/debugging. | `systematic_trading.messaging.replay`; `scripts/replay_platform_events.py`; `tests/test_event_replay.py` |
| P1.7 | Done | Queue adapter selection | Select initial broker: NATS JetStream, Redpanda, or Kafka. | Selected NATS JetStream for the initial queue adapter target. Evidence: `docs/queue-adapter-selection.md`. |
| P1.8 | Done | Real queue publisher adapter | Implement publisher protocol for selected queue. | `NatsJetStreamPublisher`; dispatcher `--publisher nats`; optional `queue` dependency; Docker Compose path in `deploy/nats/`; stream bootstrap script `scripts/configure_nats_stream.py`; `tests/test_nats_publisher.py`; full suite passed: 147 tests. Live NATS smoke passed after Docker became available. |

## P2: Service Foundation

| ID | Status | Work Item | Acceptance Criteria | Evidence / Notes |
| --- | --- | --- | --- | --- |
| P2.1 | Done | Local service supervisor plan | Operator dashboard, outbox dispatcher, automation loop, and future recorder have start/stop/restart conventions. | `docs/service-supervisor.md` |
| P2.2 | Done | Service manifest | Machine-readable list of services, ports, env vars, health checks, and logs. | `config/service-manifest.json`; `systematic_trading.services.manifest`; `tests/test_service_manifest.py` |
| P2.3 | Done | Health check contract | Every long-running service exposes health, heartbeat, and last-error state. | `systematic_trading.services.health`; `GET /health`; dispatcher state file `var/run/event_outbox_dispatcher.state.json`; `tests/test_service_health.py`; full suite passed: 146 tests. |
| P2.4 | Done | Postgres target design | Transactional schema target for approvals, orders, fills, outbox, incidents, and strategy registry. | `docs/postgres-transactional-store-design.md`; linked from `README.md` and `docs/data-contracts.md`; `git diff --check` passed. |
| P2.5 | Done | SQLite-to-Postgres adapter boundary | Storage interface or repository boundary avoids hard-coding SQLite as final state store. | Added `systematic_trading.storage.interfaces` protocol contracts and `systematic_trading.storage.create_transactional_store`; app/service/outbox/replay composition now goes through the factory; production package code no longer imports `SQLiteStore` outside the concrete storage package. SQLite remains the only implemented backend via `ST_TRANSACTIONAL_STORE_BACKEND=sqlite`; unsupported backends fail explicitly. Evidence: `tests/test_storage_factory.py`; full suite passed: 151 tests. |
| P2.6 | Done | Columnar store target design | Decide ClickHouse versus Parquet-first warehouse for analytics. | Selected ClickHouse as the serving analytical store, Parquet as the immutable archive/interchange layer, and DuckDB as the local/offline research reader. Added `docs/columnar-store-target-design.md`, `config/columnar-store.json`, `deploy/clickhouse/docker-compose.yml`, `scripts/start_clickhouse.ps1`, `scripts/stop_clickhouse.ps1`, and `scripts/smoke_clickhouse_columnar_store.ps1`. Evidence: JSON validation passed; `pytest tests/test_service_manifest.py tests/test_operator_scripts.py`; `git diff --check`. |
| P2.7 | Done | Schema registry convention | Version event/data schemas with compatibility rules. | Added code-based registry in `systematic_trading.schemas` with registered event/data contracts, JSON Schema export, duplicate registration checks, runtime event resolution, and compatibility checks. Added `docs/schema-registry-convention.md` and linked it from project docs. Evidence: `tests/test_schema_registry.py`; focused suite `pytest tests/test_schema_registry.py tests/test_events.py tests/test_event_replay.py tests/test_nats_publisher.py`; `git diff --check`. |
| P2.8 | Done | External service runtime | Running Postgres, ClickHouse, and queue locally or on server requires infrastructure choice/install. | Queue path is live-smoked with Docker Compose NATS JetStream; container `systematic-trading-nats` is running and NATS health returns `{"status":"ok"}`. Local PostgreSQL 18 service `postgresql-x64-18` is running and `pg_isready -h 127.0.0.1 -p 5432` returns accepting connections; authenticated role smoke previously passed. ClickHouse Docker Compose path is live-smoked; container `systematic-trading-clickhouse` is healthy, `http://127.0.0.1:8123/ping` returns `Ok.`, and `scripts/smoke_clickhouse_columnar_store.ps1` created a MergeTree smoke table, inserted one row, and queried `smoke_rows=1`. Local Windows E-drive bind mount failed MergeTree atomic rename semantics, so the local ClickHouse runtime uses Docker volume `clickhouse_clickhouse_data`; Parquet archive remains on D with async Z backup. |
| P2.9 | Done | Postgres transactional adapter and migration tooling | Versioned Postgres migrations create the transactional tables, a `PostgresStore` implements the current operational `TradingStore` contract, factory config can select it with `ST_TRANSACTIONAL_STORE_BACKEND=postgres`, and a smoke test proves proposals, approvals, broker records, PnL, fundamentals, and outbox behavior without using SQLite. | Added `deploy/postgres/migrations/001_initial_transactional_store.sql`, `scripts/apply_postgres_migrations.py`, `PostgresStore`, factory support, `scripts/sync_sqlite_transactional_to_postgres.py`, and `scripts/smoke_postgres_transactional_store.py`. Applied migration `001_initial_transactional_store`. Synced SQLite transactional state into Postgres: 39 instruments, 38 proposals, 7 approval decisions, 16 broker order records, 37 PnL snapshots, 955 outbox events, and 0 pending copied outbox events after suppressing migration-generated synthetic events. FastAPI smoke with `postgres + clickhouse` returned HTTP 200 for `/health`, `/api/v1/proposals`, and SPY daily bars. Local startup scripts now default to `-TransactionalStoreBackend postgres` and `-MarketDataStoreBackend clickhouse`; `AppSettings` keeps SQLite as test/fallback default. Verification: adapter smoke passed; focused tests `19 passed`; full suite `194 passed`; final affected script/manifest/storage tests `14 passed`. |

## P3: Market Data Recording And Data Quality

| ID | Status | Work Item | Acceptance Criteria | Evidence / Notes |
| --- | --- | --- | --- | --- |
| P3.1 | Done | Market data recorder contract | Define raw payload envelope, normalized event mapping, storage paths, and replay behavior. | `docs/market-data-recorder-contract.md`; linked from `README.md`, `docs/data-contracts.md`, and `docs/service-supervisor.md`; `git diff --check` passed. |
| P3.2a | Done | Recorder source and capacity spec | Define low-frequency and high-frequency recorder lanes, IB request/market-data-line limits, initial ETF universe capacity, and fallback delayed intraday sources. | Added `docs/market-data-recorder-source-plan.md` and `config/market-data-recorder-sources.json`. Evidence: `python -m json.tool config\market-data-recorder-sources.json`; 40-symbol seed sanity check; `git diff --check`. |
| P3.2 | Done | IB market data recorder v0 | Capture IB paper market data to immutable JSONL or Parquet path. | Added `systematic_trading.recorders`, `scripts/record_ib_market_data.py`, and `scripts/replay_market_data_raw.py`. Smoke tested against running IB TWS paper: `scripts/test_ib_paper_connection.py` connected to `127.0.0.1:7497`; historical-smoke SPY 5-second bars wrote 3 raw JSONL records under E hot spool and appended 3 `market_data.recorded` outbox events; replay dry-run read 3 valid records with 0 hash mismatches. Evidence: full suite `162 passed`; JSON validation; `git diff --check`. |
| P3.3 | Done | Raw data catalog | Recorded data is discoverable by source, symbol, date, and schema version. | Added append-only raw manifest/catalog entries in `systematic_trading.recorders.market_data`, wired catalog writes into the IB recorder, and added `scripts/catalog_market_data_raw.py` for rebuild/query. Rebuilt/query-tested the existing SPY IB smoke data: 3 manifest entries, 0 invalid records, 0 duplicate raw refs. Evidence: focused suite 38 passed; full suite `163 passed`; JSON validation; `git diff --check`. |
| P3.4a | In Progress | Pilot raw-data proving and audit loop | Record a small ETF pilot, audit raw/catalog coverage, inspect gaps/duplicates/freshness/disk use, and only then scale. | Operator agreed to prove the foundation before broad normalization. 2026-07-11 shakedown started core services successfully after launching Docker Desktop: watchdog required services `ok`, service graph responded with 7 nodes, ClickHouse smoke returned `smoke_rows=2`, and IB paper API smoke returned `next_valid_order_id=20`. Initial SPY historical requests returned IB 162 `Trading TWS session is connected from a different IP address` plus API subscription text, but follow-up debug showed contract resolution is healthy and historical data now works through both direct IB API probes and the recorder. Recorder wrote 3 SPY daily bars, 5 SPY 5-second bars, and 6 multi-symbol 5-second bars for SPY/QQQ/AAPL on recorder date 2026-07-11; catalog query found 14 entries, 0 invalid records, and 0 duplicate raw refs; raw replay for SPY read 10 valid records with 0 hash mismatches and 2 duplicate deterministic raw event ids from repeated smoke pulls. Added `scripts/run_market_data_recorder_service.py` and `scripts/start_market_data_recorder_service.ps1`: the recorder now starts with local platform startup by default, stays on while idle after hours/weekends, runs historical gap-fill lookback before live capture after startup/restart during market hours, and records in bounded realtime chunks. 2026-07-13 recovery fixed first-symbol historical timeout aborts, added `historical-live` HMDS buffering, added a timestamped delayed-trade callback path, made prospective capture precede synchronous recovery, exposed structured child results/feed identity, stopped orphaned child processes during shutdown, and aligned state staleness with the 300-second capture chunk. Paid `reqRealTimeBars` remains externally blocked by IB 10089/420. The testing-only `SPY/QQQ/TLT/GLD/IWM` delayed canary wrote 25 bars in 40 seconds (5 each); the first supervised chunk wrote 32 bars with per-symbol counts `7/6/7/6/6`, no missing symbols, and zero payload-hash mismatches. Exchange timestamps confirmed the expected roughly 15-minute delay. The final normal service completed daily backfill, then correctly entered `degraded` after IB began returning 10197 `No market data during competing live session`; it remains running with historical recovery scheduled and no longer reports false health. Verification: focused `18 passed`; full suite `211 passed`; PowerShell parse, JSON validation, and `git diff --check` passed apart from existing line-ending warnings. Do not expand beyond the pilot until the competing session is cleared, paid realtime API entitlement is proven, and delayed data remains excluded from trading decisions. |
| P3.4 | In Progress | Normalized bar/quote pipeline | Convert raw records and external daily loaders into columnar golden daily/intraday tables with quality flags. | Current gap-debug session resolved operator report that SPY daily bars were absent/bad for May/June 2026. Root cause: stale SQLite carry-forward rows existed in ClickHouse with flat OHLC and volume 0; missing-only backfill skipped them. Fix: backfill now repairs flat zero-volume rows when provider has a real bar and deletes flat zero-volume rows absent from provider calendars, such as 2026-05-25 and 2026-06-19 US market holidays. SPY May/June repair inserted/replaced 23 Yahoo adjusted bars and deleted 2 stale holiday artifacts; follow-up API check returned 41 rows, 0 flat zero-volume rows, and real Yahoo bars for 2026-05-29, 2026-06-01, and 2026-06-15. Full 60-day all-symbol pass inserted/repaired 884 rows then deleted stale holiday artifacts; final ClickHouse daily total 141,512 rows across 39 symbols. Verification: focused tests `9 passed`; full suite `188 passed`. Remaining before Done: source-observation layer, source precedence/disagreement checks, intraday golden bars, and gap/staleness/source-disagreement panels. |
| P3.4c | In Progress | Migrate strategy, rebalance, PnL, and backtest market-data reads to ClickHouse | Signal generation, portfolio rebalance proposal generation, dashboard valuation/PnL, and stored backtests read daily bars from ClickHouse `market_data.daily_bars` instead of SQLite `price_bars`; SQLite price endpoints are either deprecated or explicitly marked legacy. | Added `ClickHouseMarketDataStore`, `MarketDataRoutedTradingStore`, and `create_trading_store`. `ST_MARKET_DATA_STORE_BACKEND=clickhouse` routes `list_price_bars`, `upsert_price_bar`, `list_fx_rates`, and `upsert_fx_rate` to ClickHouse. Operator startup defaults to Postgres transactional state plus ClickHouse market-data backend after P2.9. Synced 3,790 SQLite FX rows into ClickHouse `market_data.fx_rates`. Live routed-store smoke returned repaired SPY 2026-06-01 close 756.5908203125 with volume 43,634,900, and USD/CNH FX through 2026-06-01. Legacy `/api/v1/market-data/bars/SPY` now returns the same ClickHouse value. SOTA rebalance CLI smoke for decision date 2026-07-10 succeeded without queuing orders. Verification: focused tests `40 passed`; full suite `190 passed` before P2.9, and `postgres + clickhouse` FastAPI smoke passed during P2.9. Remaining before Done: migrate or retire legacy research scripts that instantiate `SQLiteStore` directly, normalize intraday bars into ClickHouse, and keep SQLite as read-only fallback until operational soak is complete. |
| P3.5 | Pending | Data freshness checks | Detect stale bars, missing FX, source gaps, duplicates, and clock drift. | Alerts must be generated. |
| P3.6 | Pending | Replay a trading day | Reconstruct a paper-trading day from recorded data and compare to stored decisions. | Phase 2 exit criterion. |
| P3.7 | Blocked | Paid/primary market-data vendor | Source-of-record vendor choice and credentials are not settled. | Use IB/Yahoo/Tushare only as current interim sources. |

## P4: Research, Backtest, And Promotion Registry

| ID | Status | Work Item | Acceptance Criteria | Evidence / Notes |
| --- | --- | --- | --- | --- |
| P4.1 | Pending | Strategy spec schema | Versioned strategy spec includes universe, data, features, schedule, risk, execution, and artifacts. | Required before registry. |
| P4.2 | Pending | Strategy promotion registry | Persist state transitions from idea to retired with evidence and approval. | Should emit promotion events. |
| P4.3 | Pending | LEAN integration design | Document how current strategy definitions map into LEAN or LEAN-equivalent runs. | Avoid parallel strategy logic. |
| P4.4 | Pending | LEAN smoke backtest | One current SOTA or simplified strategy runs through LEAN path with reproducible artifacts. | Promotion path cannot depend only on custom engine. |
| P4.5 | Pending | Backtest artifact hashes | Reports, inputs, configs, and outputs are hashed and linked to strategy versions. | Required for audit. |
| P4.6 | Pending | Overfit/stability gate | Promotion requires split, benchmark, turnover, fee, stress, and parameter stability checks. | Extend current SOTA workflow. |
| P4.7 | Blocked | LEAN runtime/dependency setup | Requires installing/configuring LEAN runtime or choosing equivalent deployment path. | Not blocked conceptually, blocked operationally. |
| P4.8 | Done | Strategy catalog and theoretical performance portal | Operator can navigate all discovered strategy artifacts, identify current SOTA, inspect allocation and comparable metrics, and distinguish backtest/theoretical performance from actual portfolio performance. | Added cached read-only artifact discovery in `systematic_trading.research.catalog`, `GET /api/v1/strategies`, and `/strategies`. Live artifact smoke discovered 90 distinct usable runs, exactly one canonical SOTA, and six current-SOTA allocation positions. This is explicitly an artifact catalog until P4.2 adds governed persisted promotion state. Evidence: focused tests `35 passed`; full suite `200 passed`. |
| P4.9 | Done | Monitored strategy lifecycle and detail workspace | Strategy catalog separates monitored and archived runs; SOTA is monitored through current market data; each strategy opens a detailed performance, holdings, leverage, attribution, and benchmark view. | Added `config/strategy-monitoring.json`, Monitored/Archived registry tabs, dynamic monitored mark-to-market through golden daily bars, native strategy detail API/page, comparison metrics, NAV/benchmark chart, holdings, leverage, exposures/attribution, and generated report links. Live: 1 monitored SOTA, 89 archived; SOTA artifact end `2026-04-29`, data through `2026-07-10`, 49 extension points. Browser verified 8 metrics, 6 holdings, 3 comparison windows, 2 chart series, and report access. Full suite passed (`207 tests`). |
| P4.10 | Done | Direct current full report for monitored strategies | Clicking a monitored strategy opens the complete backtest report directly, with NAV, metrics, benchmark, holdings, and provenance refreshed through current golden data; archived reports remain immutable. | Monitored catalog rows now open an in-memory full report with current NAV, primary and MSCI World benchmarks, period metrics, drawdowns, reconstructed holdings/contribution, signal attribution, and explicit artifact-end/data-through provenance. Stored artifacts are not overwritten; archived rows retain their existing detail fallback when no HTML report exists. Live browser: SOTA artifact end `2026-04-29`, monitored through `2026-07-14`, 8 summary metrics, 2 benchmark choices, 15 yearly rows, 1,348 monthly contribution rows, signal attribution visible, and no horizontal overflow. Full suite passed (`217 tests`). |

## P5: Portfolio Blotter And Execution Controls

| ID | Status | Work Item | Acceptance Criteria | Evidence / Notes |
| --- | --- | --- | --- | --- |
| P5.1 | Pending | Rebalance blotter domain model | Model current, target, proposed orders, expected cash, FX, fees, residuals, and constraints. | Stronger than current proposal preview. |
| P5.2 | Pending | Pre-trade check engine | Checks stale data, missing FX, price bands, buying power, duplicate orders, open orders, turnover, concentration, and live caps. | Must block routing. |
| P5.3 | Pending | Blotter API | Dashboard/API can show proposal, constraints, violations, and operator decision actions. | Current dashboard can be extended. |
| P5.4 | Pending | Resize/defer workflow | Operator can approve, reject, resize, defer, or cancel generated orders. | Must persist audit events. |
| P5.5 | In Progress | IB connection health gate | Confirm server time, account, managed accounts, next valid id, and profile before routing. | Added `scripts/probe_ib_tws_health.py`, `systematic_trading.execution.ib_health`, `ib_tws_api` service manifest entry, startup/watchdog probes, and health-page state-file reporting. Local probe on 2026-07-12 correctly reported TWS/API down at `127.0.0.1:7497` with missing `nextValidId`. Remaining before Done: make order routing and recorder live capture consume a fresh healthy IB state/profile check before proceeding. |
| P5.6 | In Progress | Broker reconciliation gate | Compare IB positions, cash, open orders, fills, and commissions before routing. | Added `systematic_trading.execution.reconciliation`, `/api/v1/dashboard/reconciliation/interactive-brokers`, and `scripts/reconcile_ib_paper_account.py`. The report compares local broker order records to IB paper executions and account positions, writes a JSON report, and can record an empty PnL reset baseline only with `--record-pnl-reset-baseline --confirm-paper-reset` when IB paper positions are zero. Remaining before Done: include broker open orders/commissions, persist reconciliation runs in Postgres, and block new routing on unresolved breaks. |
| P5.6a | Done | Broker-authoritative portfolio reset workflow | Operator refreshes IB positions/cash before showing holdings, raises a durable reconciliation alarm on mismatch, and requires explicit trader confirmation before anchoring active portfolio/PnL state to the official IB snapshot without deleting execution history. | Trading page and 60-second management loop now persist fresh IB snapshots/reconciliation reports; breaks raise `alert.raised`, disable operator trading actions, block EOD PnL/rebalance staging, and server-block IB routing. Reset supports empty or non-empty IB positions through a broker-derived PnL baseline and preserves older orders/fills as audit history. Fixed unsuffixed IB execution timestamps to interpret TWS local time before converting to UTC. Live: initial IB `0` positions vs six local differences; an operator-confirmed baseline `bed6ac19a652` anchored the active account to IB; final reconciliation `matched`, HKD cash `1,015,927.30`, zero positions, zero open lots, PnL `0.00`, and management-loop report fresh with zero breaks. Focused suite `60 passed`; full suite `216 passed`; `git diff --check` passed apart from existing line-ending warnings. |
| P5.10 | Done | Rebalance execution window and missed attribution | Rebalance proposals expire after a configurable next-session-open window; approval, submission, and resubmission are server-blocked after expiry; missed requests are durable, greyed in the UI, and included in execution-quality analysis. | Added `ST_EXECUTION_REBALANCE_TIMEOUT_MINUTES` (default 30), UTC deadlines derived from next-session start in `automation_timezone`, `missed` proposal/order states, API and router gates, durable missed records, and missed count/notional/detail analysis. Live cleanup moved 32 stale proposals/192 orders to missed while preserving 6 approved proposals with routed activity. Browser verified 32 grey rows with disabled decisions and visible deadlines. Full suite passed (`207 tests`). |
| P5.7 | Pending | Live kill switch | A hard config/runtime switch prevents live order routing immediately. | Must be tested in paper. |
| P5.8 | Pending | Live capital caps | Per-account, per-strategy, per-symbol, per-order, turnover, and drawdown caps. | Required before Phase 5. |
| P5.9 | Blocked | Live account enablement | Requires explicit human approval, credentials, account id, and proven paper evidence. | Keep live disabled. |

## P6: Observability, Alerts, And Incidents

| ID | Status | Work Item | Acceptance Criteria | Evidence / Notes |
| --- | --- | --- | --- | --- |
| P6.14 | Done | Reset-aware interactive performance tracking | Readable full-history chart, separate aligned account axis, reset-aware account history, diagnosed strategy gaps, hover and period selection with statistics. | `docs/dashboard-performance.md`; 339 tests passed against the exact staged export. Desktop/390px synthetic browser verification passed keyboard, drag, presets and empty/single-series states. Audit files and execution controls preserved. |
| P6.0 | Done | Foundation service health portal and startup path | A single recommended local startup script brings up NATS, verifies Postgres, starts ClickHouse, starts the operator API/dispatcher, starts the scheduled market-data recorder service, and exposes a web portal with per-service health plus a connection graph. | Added `scripts/start_local_platform.ps1`, `scripts/stop_local_platform.ps1`, `scripts/stop_market_data_recorder.ps1`, `/platform`, and `/api/v1/platform/service-graph`. 2026-07-11 update: recorder is no longer merely opt-in; startup launches the scheduled service by default unless `-SkipMarketDataRecorder` is passed. Weekend smoke showed recorder `ok` and idle with message `Market data recorder service idle: weekend`; platform `/health` overall `ok`. Evidence: full suite latest `181 passed`; service manifest JSON validation; `git diff --check` only reported LF/CRLF warnings. |
| P6.0a | Done | Structured operational logs v0 | Core scripts and services write durable JSONL operational events with service id, level, event name, timestamp, message, and details so later agents can debug startup, health, dispatcher, and recorder behavior. | Added `systematic_trading.services.operational_log` and shared `var/log/platform_operations.jsonl`. Emitters now include `local_platform_supervisor`, `operator_dashboard`, `event_outbox_dispatcher`, and `market_data_recorder`; obvious secret fields are redacted. Recorder stop script now rewrites stopped state and logs stop/stale/no-PID cases. Smoke log wrote `dispatcher_started`, `dispatch_batch`, and `dispatcher_completed`. Evidence: full suite `169 passed`; focused observability suite `31 passed`; final focused suite `9 passed`; service manifest JSON validation; `git diff --check` only reported existing LF/CRLF warnings. |
| P6.0b | Done | Recorder/VPN/TWS incident triage and resilience hardening | Diagnose the one-day recorder run incident, document the VPN/TWS operational risk, and add local restart/health tooling so NATS, ClickHouse, and recorder failures are visible and recoverable. | Added `scripts/watch_local_platform.ps1`; watchdog writes `var/run/local_platform_watchdog.state.json` and `local_platform_watchdog` operation logs, checks NATS/Postgres/ClickHouse/operator/recorder state, and can repair required NATS/ClickHouse/operator services with `-Repair`. Added IB automation circuit breaker and alert dedupe to prevent repeated TWS logout/`nextValidId` storms. Hardened SOTA automation so non-SOTA IB account positions such as `DBB`/`USO` are filtered with warnings instead of breaking rebalance staging. Updated optional stopped-service health semantics. Updated README, service supervisor, live rollout, source plan, and `log.md`. Evidence: full suite `173 passed`; final `/health` overall `ok`; watchdog smoke required services `ok`; optional recorder `disabled`. |
| P6.0c | Done | Docker/TWS health and startup preflight hardening | Startup and watchdog detect Docker daemon-not-running failures before Docker Compose calls; platform health exposes TWS/API connectivity as an operator-visible service; remediation messages are explicit and logged. | Added `scripts/assert_docker_ready.ps1` and wired it into NATS/ClickHouse startup before `docker compose up`; startup now attempts to start Docker Desktop and wait for the daemon, then fails with a concise remediation message if the engine remains unreachable. Added TWS health state file path `var/run/ib_tws_api.state.json`, `scripts/probe_ib_tws_health.py`, startup/watchdog probes, and service manifest entry. Local TWS-down smoke wrote a health state with `running=false` and `nextValidId` timeout details. Verification: focused tests `30 passed`; script test after Docker auto-start update `1 passed`. |
| P6.1 | Pending | Metrics contract | Define service, data, queue, strategy, portfolio, execution, and reconciliation metrics. | Prometheus/OpenTelemetry compatible. |
| P6.2 | Pending | Local metrics endpoint | API/service exposes metrics for scraping or dashboard display. | Start with FastAPI endpoint if needed. |
| P6.3 | Pending | Grafana dashboard plan | Dashboard panels defined for data freshness, queue lag, broker state, PnL, exposure, and alerts. | Implementation depends on metrics path. |
| P6.4 | Done | Alert event producer | Automation alerts also emit `alert.raised` into platform outbox. | `AutomationAlertNotifier` accepts an event store and appends `AlertRaisedEvent`; default `TradingManagementService` passes its SQLite store; `tests/test_alerts.py`; full suite passed: 146 tests. |
| P6.5 | Pending | Desktop popup alert channel | Local machine popup for trading-critical warnings/errors. | Windows implementation required. |
| P6.6 | Pending | Incident registry | Alerts can open/update/resolve incidents with durable status. | Links to event ids. |
| P6.7 | Pending | Alert delivery audit | Track which channels received each alert and whether delivery failed. | Required before live. |
| P6.8 | Blocked | SMS/push provider | Requires provider choice and credentials. | Email exists; urgent channel pending. |
| P6.9 | Done | Market data audit portal | Operator can inspect historical/raw market data in chart and table form with source, symbol, date, quality flags, raw refs, and replay/audit metadata. | Added `/api/v1/market-data/audit` and `/platform/market-data-audit`. The API reads the raw catalog, dereferences raw JSONL records, verifies payload hashes, counts duplicate raw event ids, and returns chart/table-ready rows. The portal now uses ClickHouse daily bars as the primary workstation and keeps raw recorder data as secondary evidence. Evidence: live localhost page `200`; daily symbols returned 39 symbols; SPY 2026-07-01 through 2026-07-10 returned 7 ClickHouse bars; browser desktop and 390px compact checks rendered chart/table with no console warnings/errors; raw SPY query for recorder date 2026-07-11 returned 10 rows/bars, 0 hash mismatches, 0 invalid records, and 2 duplicate deterministic raw event ids from repeated smoke pulls; full suite latest `184 passed`. |
| P6.9a | Done | First-class intraday market-data view | Market Data opens directly on the latest recorder date, exposes intraday symbols and 5-second bars in the main chart/table workspace, shows delayed/live provenance and raw references, and retains Daily Bars as a store selection. | Added recorder-date discovery to `/api/v1/market-data/audit/symbols` and made Intraday Bars the default Market Data store. The main workspace now loads the latest recorder date, date-specific symbols, capture mode, bar size, delayed/live provenance, OHLCV chart, full-width table, quality flags, and raw references; exact duplicate raw event ids are removed from the main series but retained in Raw Evidence. Live browser verification loaded `2026-07-13`, pilot symbols `GLD/IWM/QQQ/SPY/TLT`, 52 unique SPY bars, 57 raw rows, 5 duplicate ids, `IB delayed`, and no horizontal overflow. Daily Bars remained selectable and loaded 3,651 SPY rows. Compact 390px verification passed. Focused API/UI tests: `10 passed`; full suite: `211 passed`. |
| P6.9b | Done | Multi-session intraday market-data view | Market Data can chart the latest 1, 5, or 10 available recorder sessions, supports explicit inclusive start/end recorder dates, and preserves chronological chart/table ordering and raw audit evidence. | Added inclusive recorder start/end filters to audit and symbol APIs while retaining exact-date compatibility. `1D/5D/10D/All` presets select available recorder partitions rather than assuming consecutive calendar days; limited API reads prefer newest matching evidence and return it chronologically. Live browser verification: partitions `2026-06-27/2026-07-11/2026-07-13`; 1D selected the latest, 5D and 10D selected all three, 5D loaded 198 unique SPY bars at final check with delayed provenance and zero desktop overflow; Daily Bars still loaded 3,651 SPY rows. Focused tests: `11 passed`; full suite: `212 passed`; `git diff --check` passed apart from existing line-ending warnings. The in-app viewport override did not apply a fresh 390px viewport, so compact verification was not claimed for this increment. |
| P6.9c | Done | Explicit market-data symbol selector | Intraday and Daily Bars expose every available symbol in a visible, selectable control rather than relying on browser-native datalist discovery. | Replaced the ambiguous datalist text input with an explicit symbol dropdown that repopulates by store and session range. Live latest-session Intraday Bars exposed `GLD/IWM/QQQ/SPY/TLT`; QQQ loaded 223 delayed bars. The broader range API also contains historical AAPL smoke data. Daily Bars exposed all 39 converted symbols; AOR loaded 3,643 rows from `2012-01-03` through `2026-07-13`. Browser checks had zero desktop overflow. Focused tests: `11 passed`; full suite: `212 passed`; `git diff --check` passed apart from existing line-ending warnings. |
| P6.10 | Done | Actual versus strategy-theoretical attribution | Operator performance shows actual portfolio PnL against the active strategy's normalized backtest NAV, with an explicit divergence series; execution slippage remains separately reported. | The existing indexed SOTA backtest NAV versus actual account NAV view is now explicitly labeled `Backtest theoretical vs actual account`, links directly to the strategy catalog, and keeps reference-fill PnL in the separate execution-quality panel. Evidence: operator/API tests cover the labels, navigation, catalog, SOTA selection, and allocation; full suite `200 passed`. |
| P6.11 | Done | Supervisor PID reuse hardening | Operator and dispatcher startup/stop paths validate process identity before trusting PID files, so recycled Windows PIDs cannot suppress startup or terminate unrelated processes. | Incident on 2026-07-13: stale operator PID `31312` was reused by Microsoft Edge; startup incorrectly reported success while port 8000 had no listener. Operator identity now requires ownership of the configured listening port; dispatcher identity uses its state-file process id. Live recovery returned HTTP 200 for `/platform`; focused script tests passed. |
| P6.12 | Done | Primary strategy navigation | Strategies is a persistent top-level navigation destination from Operator, Health, and Market Data pages, with browser-verified access to the populated strategy catalog. | Added `Strategies` to all operational page headers. Browser verification from Platform Health clicked the tab, opened `/strategies`, loaded 90 strategy runs, and selected the current SOTA detail. Focused operator UI/script tests passed (`6 passed`). |
| P6.13 | Done | Unified operational header | Operator, Strategies, Health, and Market Data pages use one identical top navigation contract; page-specific status and refresh controls live in content toolbars rather than the global header. | All headers now contain only Operator, Strategies, Health, and Market Data in the same order. Health Refresh/status moved into Service Status; market-data controls moved into filters; operator connection state moved into Automation. Browser checks confirmed zero header buttons/status elements on Health and Strategies. Focused UI/script tests passed (`7 passed`). |

## P7: Daily Operating Loops

| ID | Status | Work Item | Acceptance Criteria | Evidence / Notes |
| --- | --- | --- | --- | --- |
| P7.1 | Pending | Daily post-trade report | Generate PnL, attribution, slippage, fills, reconciliation, and alert summary. | Uses `.agents/skills/daily-post-trade-analysis.md`. |
| P7.2 | Pending | Continuous research report | Daily/weekly challenger report updates research state and candidate status. | Uses `.agents/skills/continuous-research-loop.md`. |
| P7.3 | Pending | Robustness review report | Weekly service/data/queue/broker/alert health review. | Uses `.agents/skills/system-robustness-review.md`. |
| P7.4 | Pending | Promotion review report | Formal approve/reject/defer/retire artifact for strategy transitions. | Uses `.agents/skills/strategy-promotion-control.md`. |
| P7.5 | Pending | Automated Kanban reminder | Daily loop reports include open `In Progress`, `Blocked`, and high-priority `Pending` items. | Keeps execution tracker current. |
| P7.6 | Pending | Disaster recovery drill | Backup and restore check for trading-critical local state. | Required before live. |

## Next Recommended Work Order

1. Continue P3.4c: migrate or retire legacy research/backtest scripts that instantiate `SQLiteStore` directly, then mark SQLite read-only fallback after a short Postgres soak.
2. Continue P5.5/P5.6: make order routing and recorder live capture require fresh IB health and no unresolved reconciliation breaks before proceeding.
3. Continue P3.4: add ClickHouse source-observation tables, source precedence, cross-source disagreement checks, and daily gap/source-disagreement panels.
4. During market hours, resolve P3.4a IB API market-data entitlement/session-IP blocker and rerun a 1-symbol live ETF canary before any 5-symbol or 30-40-symbol capture.
5. Start P4.1: define the versioned strategy spec schema.
6. Start P6.1: define service, data, queue, strategy, portfolio, execution, and reconciliation metrics.
7. Start P5.1: define the stronger rebalance blotter domain model.
