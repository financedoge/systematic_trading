# Market Data Recorder Contract

Status: P3.1 contract

Decision date: 2026-06-27

Source and capacity companion: `docs/market-data-recorder-source-plan.md`

## Purpose

The market data recorder is the source-of-record ingestion service for live and paper market data. Its first responsibility is to preserve raw source payloads before any normalization, event publishing, trading decision, dashboard update, or analytics write.

This contract applies to IB TWS or IB Gateway data, vendor feeds, FX, bars, quotes, trades, order-book snapshots, and broker market-data status messages.

## Non-Negotiable Rules

- Raw-before-publish: no normalized `market_data.recorded` event may be emitted until the raw payload has been durably written and can be referenced by `raw_ref`.
- Immutable raw archive: raw records are append-only. Corrections create new records with links to the prior record; they do not overwrite prior data.
- Point-in-time timestamps: every record carries source timestamp, recorder receive timestamp, and downstream availability timestamp when applicable.
- Idempotent replay: replaying the same raw record must not create duplicate logical events.
- Separation of concerns: recorder captures and minimally validates; normalizers produce canonical bars, quotes, and quality flags.
- Trading safety: stale, missing, duplicated, out-of-order, or clock-drifted data must create alerts before it can drive portfolio or execution decisions.

## Raw Envelope

Each raw record is one JSON object in JSONL v0. Parquet can replace or mirror JSONL after the columnar store decision, but the logical envelope stays stable.

Required fields:

- `schema_version`: integer, starts at `1`.
- `raw_event_id`: globally unique id generated by the recorder.
- `source`: source descriptor with `name`, `kind`, `environment`, and optional connection/profile identifiers.
- `recorder`: descriptor with `service_id`, `instance_id`, and software version when available.
- `data_kind`: `trade`, `quote`, `bar`, `order_book`, `fx`, or `broker_status`.
- `symbol`: normalized platform symbol when known.
- `currency`: quote currency when known.
- `exchange`: source exchange or venue when known.
- `exchange_timestamp`: timestamp from source/vendor when supplied.
- `received_at`: recorder UTC timestamp when the payload was received.
- `available_at`: UTC timestamp when the record became available to downstream consumers.
- `source_sequence`: vendor or broker sequence id when supplied.
- `capture_mode`: `stream`, `snapshot`, `historical_backfill`, or `replay`.
- `payload_encoding`: normally `json`; may be `text` or `base64` for source-native payloads.
- `payload`: raw source payload, minimally transformed only enough to serialize safely.
- `payload_hash`: SHA-256 hash of canonical serialized payload bytes.
- `quality_flags`: list of recorder-level quality flags, initially empty if no issue is detected.

Optional linkage fields:

- `previous_raw_event_id`: correction or replacement source record.
- `ingest_batch_id`: batch id for historical pulls or reconnect recovery.
- `request_id`: source API request id.
- `raw_ref`: storage reference to the written raw record after append.

Example:

```json
{
  "schema_version": 1,
  "raw_event_id": "raw_20260627_ib_paper_SPY_000001",
  "source": {"name": "interactive-brokers", "kind": "broker", "environment": "paper"},
  "recorder": {"service_id": "market_data_recorder", "instance_id": "local-windows"},
  "data_kind": "bar",
  "symbol": "SPY",
  "currency": "USD",
  "exchange": "SMART",
  "exchange_timestamp": "2026-06-27T20:00:00+00:00",
  "received_at": "2026-06-27T20:00:03.120000+00:00",
  "available_at": "2026-06-27T20:00:03.140000+00:00",
  "source_sequence": "92001:20260627",
  "capture_mode": "stream",
  "payload_encoding": "json",
  "payload": {"open": "512.10", "high": "513.00", "low": "511.80", "close": "512.34", "volume": "1000000"},
  "payload_hash": "sha256:...",
  "quality_flags": []
}
```

## Storage Layout

The local storage policy is `config/market-data-storage.json`.

Drive roles on this machine:

- E: fast NAND hot spool for active recorder writes and near-term replay.
- D: larger local archive for closed immutable chunks.
- Z: network-backed backup target; safer but never in the synchronous live ingest path.

Local v0 paths are rooted by the storage policy:

- Hot raw JSONL: `E:/systematic_trading_runtime/market-data/hot/raw/source=<source>/environment=<env>/data_kind=<kind>/date=<YYYY-MM-DD>/symbol=<SYMBOL>/part-<UTC>.jsonl`
- Local archive: `D:/systematic_trading_data/market-data/raw/source=<source>/environment=<env>/data_kind=<kind>/date=<YYYY-MM-DD>/symbol=<SYMBOL>/part-<UTC>.jsonl`
- Backup archive: `Z:/systematic_trading_backup/market-data/raw/source=<source>/environment=<env>/data_kind=<kind>/date=<YYYY-MM-DD>/symbol=<SYMBOL>/part-<UTC>.jsonl`
- Raw manifest: `<root>/raw/_manifest/date=<YYYY-MM-DD>.jsonl`
- Normalized JSONL or Parquet staging: `<root>/normalized/<kind>/date=<YYYY-MM-DD>/symbol=<SYMBOL>/`
- Replay reports: `var/reports/market-data-replay/<YYYY-MM-DD>/`
- Service state: `var/run/market_data_recorder.state.json`
- Service logs: `var/log/market_data_recorder.out.log` and `var/log/market_data_recorder.err.log`

Bulk market data must not be stored inside the git repository tree except for tiny test fixtures.

Tiering rule:

1. Write active raw chunks to the E: hot spool.
2. Close and checksum chunks.
3. Copy closed chunks to the D: local archive.
4. Mirror closed chunks to Z: asynchronously.
5. Delete hot-spool chunks only after D: archive copy and checksum verification pass. Z: backup success is desirable but must not block live ingest.

Watermarks from `config/market-data-storage.json`:

- Warn if E: hot-spool free space drops below 100 GB.
- Stop new recorder subscriptions if E: hot-spool free space drops below 60 GB.
- Warn if D: archive free space drops below 200 GB.
- Stop archive promotion if D: archive free space drops below 100 GB.
- Warn if Z: backup free space drops below 300 GB.

Raw file write rule:

1. Build the raw envelope without `raw_ref`.
2. Append it to the target raw file.
3. Flush the append and record the byte offset or line number.
4. Populate `raw_ref` as `path#line=<line>` or `path#offset=<byte_offset>`.
5. Only then create normalized records and append platform events.

## Raw Data Catalog

Status: P3.3 catalog

The raw catalog is an append-only manifest that makes recorded files discoverable without scanning every raw partition. Manifests live under the active storage root:

- Hot manifest: `E:/systematic_trading_runtime/market-data/hot/raw/_manifest/date=<YYYY-MM-DD>.jsonl`
- Local archive manifest: `D:/systematic_trading_data/market-data/raw/_manifest/date=<YYYY-MM-DD>.jsonl`
- Backup manifest: `Z:/systematic_trading_backup/market-data/raw/_manifest/date=<YYYY-MM-DD>.jsonl`

Each manifest row records:

- Catalog schema version.
- Raw schema version.
- Source name, kind, environment, data kind, symbol, currency, and exchange.
- Recorder partition date and source exchange date/timestamp when available.
- `raw_event_id`, `raw_ref`, raw path, byte offset, byte count, and payload hash.
- Capture mode, request id, received/available timestamps, and quality flags.

Catalog rules:

1. The recorder writes the raw JSONL record first.
2. The recorder appends the raw catalog entry second.
3. The recorder appends `market_data.recorded` to the event outbox third.
4. Catalog manifests are append-only; query code de-duplicates by `raw_ref`.
5. Missing or damaged manifests can be rebuilt by scanning raw files and validating the raw envelope.

Catalog query/rebuild command:

```powershell
.\.venv\Scripts\python.exe .\scripts\catalog_market_data_raw.py `
  --rebuild `
  --source interactive-brokers `
  --environment paper `
  --data-kind bar `
  --date 2026-06-27 `
  --symbol SPY
```

## Normalized Event Mapping

Raw records map to `MarketDataRecordedEvent` in `systematic_trading.domain.events`.

Required mapping:

- `event_id`: deterministic id derived from `raw_event_id` for idempotent replay, for example `market_data.recorded.<raw_event_id>`.
- `event_type`: `market_data.recorded`.
- `source.service`: `market-data-recorder`.
- `source.environment`: paper or live.
- `occurred_at`: source timestamp when reliable; otherwise recorder `received_at`.
- `payload.source_name`: raw `source.name`.
- `payload.data_kind`: raw `data_kind`.
- `payload.symbol`: raw `symbol`.
- `payload.currency`: raw `currency`.
- `payload.exchange_timestamp`: raw `exchange_timestamp`.
- `payload.received_at`: raw `received_at`.
- `payload.source_sequence`: raw `source_sequence`.
- `payload.raw_ref`: raw storage reference.
- `payload.normalized_ref`: normalized storage reference when a normalized artifact exists.
- `payload.quality_flags`: recorder and normalizer flags.

Bar records additionally map `price` to close price and `volume` to source volume. Quote records map bid and ask. FX records map `currency` and price/rate according to the canonical FX model.

## Replay Contract

Replay must support:

- Filter by source, environment, data kind, symbol, and UTC/date range.
- Validate `payload_hash` before use.
- Rebuild `MarketDataRecordedEvent` objects without requiring live source connectivity.
- Dry-run mode that reports counts, gaps, duplicates, and schema errors without appending to the outbox.
- Publish mode that appends replayed events to the platform outbox with deterministic event ids.
- Comparison mode that checks replayed events against stored decisions, proposals, fills, and post-trade reports.

Replay must not:

- Change raw files.
- Hide schema errors by silently dropping records.
- Use current database bars as the source of truth when raw records exist.

## Quality Flags

Initial quality flags:

- `source_timestamp_missing`
- `received_before_exchange_timestamp`
- `clock_drift`
- `duplicate_source_sequence`
- `out_of_order`
- `stale`
- `gap_detected`
- `zero_or_negative_price`
- `crossed_quote`
- `empty_payload`
- `schema_error`
- `carried_forward`
- `source_reconnect`

Flags that can affect trading decisions must also create `alert.raised` events through the platform outbox.

## Service Health

The recorder state file must include:

- `service_id`: `market_data_recorder`.
- `running`.
- `started_at`.
- `heartbeat_at`.
- `last_error`.
- `last_received_at`.
- `last_raw_ref`.
- `last_event_id`.
- `symbols_active`.
- `records_received`.
- `records_written`.
- `events_appended`.
- `quality_flag_counts`.

The service manifest already reserves `var/run/market_data_recorder.state.json` for this check.

## P3.2 Implementation Slice

The first implementation should be deliberately narrow and must follow the source, pacing, and ETF-universe limits in `docs/market-data-recorder-source-plan.md`:

1. Record IB paper 5-second real-time bars for the configured ETF seed list.
2. Write raw JSONL using this envelope.
3. Append `market_data.recorded` events to the local outbox only after raw write success.
4. Write the recorder state file.
5. Add replay dry-run for one day and one symbol.
6. Keep top-of-book quotes behind a config flag until market-data-line use is probed.
7. Keep tick-by-tick recording limited to the canary set.
8. Keep live trading disabled.

Python is acceptable for this slice. Revisit Go/Rust/C++ only after measured recorder latency, throughput, or reliability requires it.
